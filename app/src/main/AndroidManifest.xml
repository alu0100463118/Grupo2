<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.grupo2.aplicacion_grupo2" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".Actividad_Principal"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Termometro"
            android:label="Termometro">
        </activity>
        <activity android:name=".Pulsometro"
            android:label="Pulsometro">
        </activity>
        <activity android:name=".Luz"
            android:label="Sensor de Luz">
        </activity>
    </application>

</manifest>
